version: 2.1
jobs:
  linux-unit-tests:
    docker:
      - image: swift:5.2
    steps:
      - checkout
      - run: 
          name: Run Unit tests and deploy coverage
          command: ./coverage.sh

workflows:
  version: 2
  tests:
    jobs:
      - linux-unit-tests